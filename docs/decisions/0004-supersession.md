# ADR 0004: Supersession And Tombstones

**Decision:** Corrections create a new active fact linked to an atomically superseded fact; forgetting tombstones the active row.

Current context stays unambiguous while provenance and correction history remain inspectable. Tombstones are not represented as full regulatory erasure.
