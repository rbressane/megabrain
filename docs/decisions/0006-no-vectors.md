# ADR 0006: No Vectors In V0

**Decision:** Use deterministic lexical matching and a small explicit alias map.

This is explainable, testable, and sufficient for the V0 acceptance task. Embeddings add privacy and operational complexity without a failing acceptance test that requires them.
