interface:
  display_name: "MegaBrain"
  short_description: "Use a private Git-synced Markdown brain"
  default_prompt: "Use $megabrain to set up, connect, update, or use my private brain, retrieve relevant durable context, complete the task, and save new durable learning."

policy:
  allow_implicit_invocation: true
