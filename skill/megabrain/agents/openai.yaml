interface:
  display_name: "MegaBrain"
  short_description: "Use private Brain memory and encrypted Vault"
  default_prompt: "Use $megabrain to retrieve relevant private Brain context or request agent-safe masked Vault metadata."
