interface:
  display_name: "MegaBrain"
  short_description: "Use private Brain memory and encrypted Vault"
  default_prompt: "Use $megabrain to retrieve relevant private Brain context or safely manage my local encrypted Vault."
